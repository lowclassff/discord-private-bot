#!/usr/bin/env bash
# v2d - small helper to run v2.py locally with .env
set -euo pipefail
if [ ! -f .env ]; then
  echo ".env not found. Copy .env.example to .env and edit it." >&2
  exit 1
fi
export $(grep -v '^#' .env | xargs)
python3 v2.py
